<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Align with JS dark theme default — avoids brief light flash when opening Chat / Profile -->
        <item name="android:windowBackground">@color/app_window_background</item>
        <item name="android:navigationBarColor">@color/app_window_background</item>
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>
    
    <!-- Include splash screen theme -->
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- Income library: full-screen incoming call (WhatsApp-like) -->
    <style name="incomingCall" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">#000000</item>
    </style>

</resources>
